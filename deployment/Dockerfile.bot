FROM dailyco/pipecat-base:latest

# Enable bytecode compilation
ENV UV_COMPILE_BYTECODE=1

# Copy from the cache instead of linking since it's a mounted volume
ENV UV_LINK_MODE=copy

# Print a summary of the features available in the base image (for debugging)
ENV PCC_LOG_FEATURES_SUMMARY=true

ENV PYTHONPATH=/app

# Install Deno for running edge functions locally
ENV DENO_INSTALL=/root/.deno
RUN apt-get update && apt-get install -y --no-install-recommends curl unzip \
    && curl -fsSL https://deno.land/install.sh | sh \
    && apt-get purge -y curl unzip && apt-get autoremove -y \
    && rm -rf /var/lib/apt/lists/*
ENV PATH="${DENO_INSTALL}/bin:${PATH}"

RUN --mount=type=cache,target=/root/.cache/uv \
    --mount=type=bind,source=uv.lock,target=uv.lock \
    --mount=type=bind,source=pyproject.toml,target=pyproject.toml \
    uv sync --locked --no-install-project --no-dev --group bot --group tracing

# Copy LICENSE (required by pyproject.toml)
COPY LICENSE ./

# Copy the application code and bake in the version from pyproject.toml
COPY pyproject.toml /tmp/pyproject.toml
RUN mkdir -p gradientbang \
    && VERSION=$(grep '^version' /tmp/pyproject.toml | head -1 | sed 's/.*"\(.*\)"/\1/') \
    && echo "__version__ = \"${VERSION}\"" > gradientbang/_version.py \
    && rm /tmp/pyproject.toml
COPY src/gradientbang/utils/ gradientbang/utils/
COPY src/gradientbang/scripts/ gradientbang/scripts/
COPY src/gradientbang/prompts/ gradientbang/prompts/
COPY src/gradientbang/tools/ gradientbang/tools/
COPY src/gradientbang/subagents/ gradientbang/subagents/
COPY src/gradientbang/pipecat_server/ gradientbang/pipecat_server/

COPY src/gradientbang/pipecat_server/bot.py bot.py

# Copy edge function source for local API server
COPY deployment/supabase/functions/ /app/edge-functions/

# Pre-compile all edge functions and dependencies at build time.
# server.ts uses dynamic imports, so we also cache each function's index.ts
# to eliminate first-request compilation latency.
RUN cd /app/edge-functions \
    && deno cache server.ts 2>/dev/null || true \
    && for f in */index.ts; do deno cache "$f" 2>/dev/null || true; done
