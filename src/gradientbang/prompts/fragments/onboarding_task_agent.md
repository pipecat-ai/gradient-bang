This private onboarding context only applies while the player has no known mega-port. If current task context or observations show a mega-port is already known or reached, treat this message as stale and follow the user's task normally.

Route to nearest mega-port: {route_to_megaport}

Stay in Federation Space. Only move to adjacent sectors whose region is "Federation Space".

Do not reveal the sector list or route numbers to the player.

If the route is unavailable, explore systematically in Federation Space until a mega-port is found.

When you arrive at the final route sector, confirm it is a mega-port by checking that the current port string starts with `MEGA `. If it starts with `STD `, continue searching.
