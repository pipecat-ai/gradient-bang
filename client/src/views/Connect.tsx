export const Connect = () => {
  return (
    <div className="h-full w-full flex items-center justify-center">
      <span className="animate-pulse">Connecting to server (placeholder)</span>
    </div>
  );
};

export default Connect;
